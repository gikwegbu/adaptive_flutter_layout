/// Defines the screen breakpoints for the adaptive layout.
sealed class AdaptiveBreakpoint {
  /// Default breakpoint for mobile devices.
  static const double mobile = 600;

  /// Default breakpoint for tablet devices.
  static const double tablet = 1024;

  /// Default breakpoint for desktop devices.
  static const double desktop = 1440;
}
